Подключись к репозиторию `saitkharkov-cyber/home-energetika`.

Рабочий проект: `framework-standardization`.

Ветка: `main`.

Прочитай `framework-standardization/docs/START_HERE.md`.

Полностью выполни определённый там startup flow.

Представь предусмотренный этим startup flow отчёт.

До отдельного пользовательского `+` не изменяй файлы, не выполняй implementation, staging, commit, push или другие изменения состояния.

После отчёта остановись.
