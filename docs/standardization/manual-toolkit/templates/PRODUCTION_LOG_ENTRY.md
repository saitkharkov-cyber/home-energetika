===== PRODUCTION LOG ENTRY =====

Дата:

Характеристика:

Категория:

`category_id`:

`language_id`:

Область:
Только товары, непосредственно связанные с указанной категорией.

Канонический атрибут:

Aliases / старые атрибуты:

Состояние до:
- строк канона:
- строк aliases:
- товаров:
- пустых:
- дублей:
- конфликтов:

Принятое решение:

Что изменено:

Ожидаемое количество строк:

Фактически изменено:

Состояние после:
- строк канона:
- товаров:
- остаток aliases:
- пустых:
- дублей:
- неканонических:
- конфликтов:

Исключения:

Что намеренно не выполнялось:
- глобальные определения атрибутов не удалялись;
- данные вне прямого охвата категории не изменялись;
- файлы сайта не изменялись;
- кэш не перестраивался;
- неоднозначные значения предположительно не изменялись.

Итоговый статус:

===== END LOG ENTRY =====
